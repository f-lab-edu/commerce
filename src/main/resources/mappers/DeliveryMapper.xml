<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.flab.commerce.domain.delivery.DeliveryMapper">

  <insert id="save" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO delivery(recipient, address, address_detail, zip, phone, status, order_id, rider_id)
    VALUES (#{recipient}, #{address}, #{addressDetail}, #{zip}, #{phone}, #{status}, #{orderId}, #{riderId})
  </insert>
</mapper>
