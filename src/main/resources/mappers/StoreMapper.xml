<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.flab.commerce.domain.store.StoreMapper">

    <insert id="register" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store(name, phone, address, status, description, image, owner_id, create_date_time, update_date_time)
        VALUES (#{name}, #{phone}, #{address}, #{status}, #{description}, #{image}, #{ownerId}, #{createDateTime}, #{updateDateTime})
    </insert>
</mapper>
