<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.flab.commerce.domain.order.OrderMapper">

  <insert id="save" useGeneratedKeys="true" keyProperty="id">
<<<<<<< HEAD
    INSERT INTO orders(address, total_price, status, menu_options, create_date_time, update_date_time)
    VALUES (#{address}, #{totalPrice}, #{status}, #{menuOptions}, #{createDateTime}, #{updateDateTime})
=======
    INSERT INTO orders(total_price, status, menu_options, user_id, create_date_time,
                       modify_date_time)
    VALUES (#{totalPrice}, #{status}, #{menuOptions}, #{userId}, #{createDateTime},
            #{modifyDateTime});
>>>>>>> 1c56d19... #40 주문에 옵션추가
  </insert>

  <select id="findById" resultType="com.flab.commerce.domain.order.Orders">
    SELECT *
    FROM orders
    WHERE id = #{id};
  </select>
</mapper>
